# ---------------------------------------------------------------------------
# Best-effort wheelhouse extras. targets/target-310p/provision.sh downloads these one
# at a time and only warns when one has no cp310 aarch64 wheel, because none of
# them is needed for `import vllm_ascend` or for `vllm serve` on an Ascend
# 310P3.
#
#   triton-ascend      published only on Huawei's mirror, and upstream's own
#                      Dockerfile.310p uninstalls it right after installing
#                      vllm-ascend, so it is deliberately not a core dependency
#   arctic-inference   speculative-decoding extra; vllm-ascend pins 0.1.1 but
#                      PyPI only publishes 0.0.x, so this one always misses
#   modelscope         alternative (non-HF) model download source
#   ray                multi-node serving
# ---------------------------------------------------------------------------
arctic-inference==0.1.1
modelscope
ray>=2.47.1,<=2.48.0
